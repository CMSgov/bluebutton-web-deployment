# {{ ansible_managed }}
127.0.0.1   localhost localhost.localdomain localhost4 localhost4.localdomain4
::1         localhost localhost.localdomain localhost6 localhost6.localdomain6

# The following lines are desirable for IPv6 capable hosts.
fe00::0     ip6-localnet
ff00::0     ip6-mcastprefix
ff02::1     ip6-allnodes
ff02::2     ip6-allrouters


{% for host in hostvars %}
{{ host }}  {{ hostvars[host]['ansible_default_ipv4']['address'] }}

{% set host_ipv4_last_octet = host.split('.')[3] %}
#HOSTNAME:  {{ hostvars[host]['ansible_env']['HOSTNAME'] }}

# {{  splunk_name_mapper[hostvars[host]['group_names'][0]]|lower }}-{{ host_ipv4_last_octet }}.{{ external_dns_name }}

{% endfor %}